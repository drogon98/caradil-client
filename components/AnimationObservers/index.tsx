import React, { ReactChild } from "react";

interface AnimationObserverProps {
  children: ReactChild;
}

export default function AnimationObserver(props: AnimationObserverProps) {
  return <div></div>;
}
